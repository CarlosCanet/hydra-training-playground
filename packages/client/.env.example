# Backend URL for the Express server
VITE_BACKEND_URL=http://localhost:5000

# Allowed hosts for Vite dev server (comma-separated if multiple)
# Leave empty for localhost only, or add your domain/IP
VITE_ALLOWED_HOSTS=your-domain.duckdns.org,192.168.1.100